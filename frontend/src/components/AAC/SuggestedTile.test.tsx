import "@testing-library/react"
import{render} from "@testing-library/react"
import SuggestedTiles from "./SuggestedTile"

describe("Renders placeholder text for suggested tiles", ()=>{
    it("should render", ()=>{
        const renderPage = render(SuggestedTiles)
        
    })
})